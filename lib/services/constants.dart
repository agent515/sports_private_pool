import 'package:flutter/material.dart';

// BottomNavigationBarItems will now act as Tabs with their own TabNavigator
enum TabItem { home, join, profile }

const Color kVioletColor = Color(0xff7303c0);
const Color kDeepBlue = Color(0xff000046);
const Color kNeonBlue = Color(0xff1CB5E0);
